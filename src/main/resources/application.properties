# ==============================================================
# CONFIGURACIÓN PREDETERMINADA (DEVELOP / NUBE)
# ==============================================================
# Base de Datos: Neon Tech (Compartida)
spring.datasource.url=${DATABASE_URL:jdbc:postgresql://ep-withered-voice-abahsoss-pooler.eu-west-2.aws.neon.tech/vitsync-testing?sslmode=require}
spring.datasource.username=${DATABASE_USERNAME:neondb_owner}
spring.datasource.password=${DATABASE_PASSWORD:npg_8mdVhbG6Yojk}
spring.datasource.driver-class-name=org.postgresql.Driver

spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true

# CORS: Permitir Frontend (Vercel + Localhost)
# Esto permite que el frontend local (localhost:5173) se conecte a este backend.
app.cors.allowed-origins=${CORS_ALLOWED_ORIGINS:https://vitsync-web-app-testing.vercel.app,http://localhost:5173,http://localhost:8080,https://vitsync.es,https://www.vitsync.es}

# Seguridad: JWT
jwt.secret=${JWT_SECRET:9a4f2c8d3b7a1e6f4a5c3b2d1e9f8a0b5c7d2e4f6a8b1c3d5e7f9a2b4c6d8e0f}
jwt.expiration=${JWT_EXPIRATION:86400000}

# Servidor
server.port=${PORT:8080}

# Email: Resend API
resend.api.key=${RESEND_API_KEY:re_Si6N14ka_2KEwK49c1juWFKjVUency8t3}
vitsync.email.from=${MAIL_FROM_ADDRESS:VitSync <no-reply@vitsync.es>}
